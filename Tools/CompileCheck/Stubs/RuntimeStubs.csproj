<Project Sdk="Microsoft.NET.Sdk">
  <!-- Signatures of the package APIs the game uses (TextMeshPro, URP, Input System). -->
  <PropertyGroup>
    <AssemblyName>RuntimeStubs</AssemblyName>
    <DefineConstants>$(UnityDefines);UNITY_EDITOR</DefineConstants>
  </PropertyGroup>
  <ItemGroup>
    <Compile Include="*.cs" />
  </ItemGroup>
</Project>
